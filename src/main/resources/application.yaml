spring:
  application:
    name: notification-service
  config:
    import: "optional:configserver:http://3.109.184.195:8888"
  kafka:
    consumer:
      bootstrap-servers: 15.206.80.123:9092 # Update with the correct IP address and port
      group-id: OneMoreRep-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring:
          json:
            value:
              default:
                type: 'com.ashish.notificationservice.dto.MsgDTO'
            type:
              mapping: 'com.ashish.planservice.dto.MsgDTO:com.ashish.notificationservice.dto.MsgDTO'

  mail:
    host: smtp.gmail.com
    username: onemorerep24x7@gmail.com
    port: 587
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true
    password: ddsziehyitvjrrqk

server:
  port: 8084

eureka:
  instance:
    host: localhost
  client:
    service-url:
      defaultZone: http://3.109.184.195:8761/eureka